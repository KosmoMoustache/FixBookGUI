//? if 1.20.1 || 1.21 || 1.21.1 {
/*package net.kosmo.fixbookgui.fabric.mixins;

import net.fabricmc.loader.api.FabricLoader;
import net.kosmo.fixbookgui.common.mixins.plugin.MixinConfigPlugin;

public class PlatformMixinPlugin extends MixinConfigPlugin {
    @Override
    protected boolean isModLoaded(String modId) {
        return FabricLoader.getInstance().isModLoaded(modId);
    }
}
*///? }
